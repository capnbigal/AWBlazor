@using Microsoft.AspNetCore.Components
@using AWBlazorApp.Components.Account.Shared
@layout AccountLayout
@attribute [System.Diagnostics.DebuggerStepThrough]

@* [ExcludeFromInteractiveRouting] is declared on each individual page rather than inherited
   here. The .NET 9 default template uses per-page declarations and we match that pattern to
   avoid any uncertainty about whether the inheritance is being honored by the conditional
   render mode in App.razor. *@
