@page "/enterprise/assets/history"
@page "/enterprise/assets/history/{SlotId:int}"
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>Asset history@(SlotId is null ? "" : $" — #{SlotId}")</PageTitle>

<MudStack Spacing="3">
    <MudStack Row="true" AlignItems="AlignItems.Center">
        <MudIconButton Icon="@Icons.Material.Filled.ArrowBack" Size="Size.Medium"
                       OnClick="@(() => Navigation.NavigateTo("enterprise/assets"))" />
        <MudText Typo="Typo.h4">@(SlotId is null ? "Asset history" : $"History for asset #{SlotId}")</MudText>
        <MudSpacer />
        @if (SlotId is not null)
        {
            <MudButton Variant="Variant.Outlined" Color="Color.Secondary" Href="enterprise/assets/history">All history</MudButton>
        }
    </MudStack>

    <AuditHistoryGrid EntityType="Asset" EntityId="@(SlotId?.ToString())" />
</MudStack>

@code {
    [Parameter] public int? SlotId { get; set; }
}
