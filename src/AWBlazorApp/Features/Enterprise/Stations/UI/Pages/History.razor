@page "/enterprise/stations/history"
@page "/enterprise/stations/history/{SlotId:int}"
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>Station history@(SlotId is null ? "" : $" — #{SlotId}")</PageTitle>

<MudStack Spacing="3">
    <MudStack Row="true" AlignItems="AlignItems.Center">
        <MudIconButton Icon="@Icons.Material.Filled.ArrowBack" Size="Size.Medium"
                       OnClick="@(() => Navigation.NavigateTo("enterprise/stations"))" />
        <MudText Typo="Typo.h4">@(SlotId is null ? "Station history" : $"History for station #{SlotId}")</MudText>
        <MudSpacer />
        @if (SlotId is not null)
        {
            <MudButton Variant="Variant.Outlined" Color="Color.Secondary" Href="enterprise/stations/history">All history</MudButton>
        }
    </MudStack>

    <AuditHistoryGrid EntityType="Station" EntityId="@(SlotId?.ToString())" />
</MudStack>

@code {
    [Parameter] public int? SlotId { get; set; }
}
