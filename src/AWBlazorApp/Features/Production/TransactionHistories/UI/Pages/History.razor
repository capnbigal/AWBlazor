@page "/aw/transaction-histories/history"
@page "/aw/transaction-histories/history/{SlotId:int}"
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>Transaction history audit</PageTitle>

<MudStack Spacing="3">
    <MudStack Row="true" AlignItems="AlignItems.Center">
        <MudIconButton Icon="@Icons.Material.Filled.ArrowBack" aria-label="Back" Size="Size.Medium" OnClick="@(() => Navigation.NavigateTo("aw/transaction-histories"))" />

        <MudText Typo="Typo.h4">@(SlotId is null ? "Transaction history audit" : $"History for TransactionHistory #{SlotId}")</MudText>
    </MudStack>

    <AuditHistoryGrid EntityType="TransactionHistory" EntityId="@(SlotId?.ToString())" />
</MudStack>

@code {
    [Parameter] public int? SlotId { get; set; }
}
