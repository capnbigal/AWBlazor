@page "/aw/sales-tax-rates/history"
@page "/aw/sales-tax-rates/history/{SlotId:int}"
@attribute [Authorize]
@inject NavigationManager Navigation

<PageTitle>Sales tax rate history</PageTitle>

<MudStack Spacing="3">
    <MudStack Row="true" AlignItems="AlignItems.Center">
        <MudIconButton Icon="@Icons.Material.Filled.ArrowBack" aria-label="Back" Size="Size.Medium" OnClick="@(() => Navigation.NavigateTo("aw/sales-tax-rates"))" />

        <MudText Typo="Typo.h4">@(SlotId is null ? "Sales tax rate history" : $"History for SalesTaxRate #{SlotId}")</MudText>
    </MudStack>

    <AuditHistoryGrid EntityType="SalesTaxRate" EntityId="@(SlotId?.ToString())" />
</MudStack>

@code {
    [Parameter] public int? SlotId { get; set; }
}
