@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(
            $"Account/Login?ReturnUrl={Uri.EscapeDataString(new Uri(NavigationManager.Uri).PathAndQuery)}",
            forceLoad: true);
    }
}
